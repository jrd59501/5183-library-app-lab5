name: Build and deploy Node.js app with React (Vite) frontend to Azure Web App - app-lab06-jrd

on:
  push:
    branches:
      - lab6-ready-jrd
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22.x'

      # Install backend dependencies
      - name: Install backend dependencies
        run: npm ci

      # Build the React frontend (Vite)
      - name: Build React app (Vite)
        run: |
          npm --prefix web ci
          npm --prefix web run build

      # Verify Vite build output
      - name: List Vite build output
        run: ls -la web/dist

      # Upload build artifact for deploy job
      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: .

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      # Download artifact from build job
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: node-app

      # Azure Login (required before deploy)
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_STAGING }}

      # Deploy to the staging slot
      - name: Deploy to Azure Web App (staging)
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'app-lab06-jrd'
          slot-name: 'staging'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_STAGING }}
          package: .
